# 프로젝트 분석 문서 — "막아라! 무너트려라!"

> 포트폴리오용 프로젝트 분석 자료

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| **프로젝트명** | 막아라! 무너트려라! (Defence Game) |
| **버전** | v1.24.5 |
| **개발 기간** | 2026.01.15 ~ 2026.02.16 (약 33일) |
| **개발 인원** | 1인 (풀스택) |
| **총 커밋 수** | 388회 (일 평균 약 12회) |
| **코드 규모** | TypeScript 206파일, 약 68,800줄 |
| **장르** | 실시간 전략(RTS) + 액션 RPG 하이브리드 |
| **플레이 형태** | 싱글 플레이 / 1v1 PvP / 2~4인 협동(Co-op) |

### 한 줄 소개

RTS와 RPG 두 가지 게임 모드를 지원하는 **풀스택 실시간 멀티플레이어 웹 게임**으로, 서버 권위(Server-Authoritative) 아키텍처 기반의 WebSocket 실시간 통신, Canvas 2D 렌더링, 적응형 보간 시스템 등을 직접 설계·구현했습니다.

---

## 2. 기술 스택

### 프론트엔드

| 기술 | 버전 | 선택 이유 |
|------|------|----------|
| **React** | 19.x | 컴포넌트 기반 UI 설계, 풍부한 생태계 |
| **TypeScript** | 5.9 | 206개 파일 규모의 코드베이스에서 타입 안정성 확보 |
| **Vite** | 7.x | HMR 기반 빠른 개발 환경, ESM 네이티브 빌드 |
| **Zustand** | 5.x | 경량 전역 상태 관리, 선택적 구독으로 불필요한 리렌더링 방지 |
| **Tailwind CSS** | 4.x | 유틸리티 퍼스트 스타일링, 빠른 UI 개발 |
| **Canvas 2D API** | 네이티브 | 60fps 게임 렌더링, 커스텀 파티클 이펙트 |
| **React Router** | 7.x | SPA 클라이언트 라우팅 |
| **Recharts** | 3.x | 관리자 대시보드 데이터 시각화 |
| **Google Analytics** | gtag.js | 사용자 행동 분석 |

### 백엔드

| 기술 | 버전 | 선택 이유 |
|------|------|----------|
| **Node.js** | 22.x | 이벤트 루프 기반 비동기 I/O, WebSocket과의 호환성 |
| **Express** | 4.x | REST API 엔드포인트 (인증, 프로필, 랭킹 등) |
| **ws** | 8.x | 네이티브 WebSocket 서버, 낮은 오버헤드 |
| **Supabase** | 2.x | PostgreSQL 기반 인증·데이터베이스 (Auth, DB, Row Level Security) |
| **JWT + bcrypt** | - | 토큰 기반 인증, 비밀번호 해싱 |
| **Helmet** | 8.x | HTTP 보안 헤더 자동 설정 |
| **TypeScript** | 5.9 | 서버-클라이언트 간 공유 타입으로 일관성 보장 |

### 인프라 & 도구

| 기술 | 용도 |
|------|------|
| **Supabase (Cloud)** | PostgreSQL DB, 인증, 실시간 API |
| **Railway / Render** | 서버 배포 (render.yaml 설정 포함) |
| **ESLint** | 코드 품질 관리 |
| **npm workspaces** | 모노레포 구조 (client + server) |
| **concurrently** | 클라이언트·서버 동시 개발 실행 |

---

## 3. 시스템 아키텍처

### 3.1 전체 구조

```
┌─────────────────────────────────────────────────────────┐
│                    Client (React + Vite)                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────┐  │
│  │  Zustand  │  │  Canvas  │  │   React  │  │ Touch  │  │
│  │  Stores   │  │ Renderer │  │   UI     │  │Controls│  │
│  └─────┬────┘  └─────┬────┘  └──────────┘  └────────┘  │
│        │              │                                   │
│  ┌─────┴──────────────┴────┐                             │
│  │    WebSocket Client     │                             │
│  └────────────┬────────────┘                             │
└───────────────┼─────────────────────────────────────────┘
                │  WebSocket (ws://)
                │  입력 전송 ↑  ↓ 게임 상태 수신
┌───────────────┼─────────────────────────────────────────┐
│  ┌────────────┴────────────┐     Server (Node.js)       │
│  │   WebSocket Server      │                             │
│  └────────────┬────────────┘                             │
│  ┌────────────┴────────────┐  ┌──────────────────────┐  │
│  │    Message Handler      │  │    Express REST API   │  │
│  └────────────┬────────────┘  │  (Auth/Profile/Rank)  │  │
│  ┌────────────┴────────────┐  └──────────┬───────────┘  │
│  │  RPG Server Game Engine │             │              │
│  │  (60fps 게임 루프)       │             │              │
│  │  ┌────────┐ ┌─────────┐ │  ┌──────────┴───────────┐  │
│  │  │Hero    │ │Enemy    │ │  │     Supabase         │  │
│  │  │System  │ │System   │ │  │  (Auth + Database)   │  │
│  │  ├────────┤ ├─────────┤ │  └──────────────────────┘  │
│  │  │Skill   │ │Boss     │ │                             │
│  │  │System  │ │System   │ │  ┌──────────────────────┐  │
│  │  ├────────┤ ├─────────┤ │  │  Friend / DM /       │  │
│  │  │Game    │ │Spawn    │ │  │  Room Manager        │  │
│  │  │Systems │ │System   │ │  └──────────────────────┘  │
│  └──────────────────────────┘                            │
└─────────────────────────────────────────────────────────┘
```

### 3.2 서버 권위 모델 (Server-Authoritative Architecture)

**핵심 설계 철학**: 모든 게임 로직은 서버에서만 실행되며, 클라이언트는 입력 전송과 렌더링만 담당합니다.

```
Client A ──[입력: 이동/스킬]──→ Server ──[게임 상태]──→ Client A
Client B ──[입력: 이동/스킬]──→ Server ──[게임 상태]──→ Client B
Client C ──[입력: 이동/스킬]──→ Server ──[게임 상태]──→ Client C
Client D ──[입력: 이동/스킬]──→ Server ──[게임 상태]──→ Client D
```

| 구분 | 서버 | 클라이언트 |
|------|------|-----------|
| 게임 루프 | 60fps (16.67ms) 틱 | 60fps 렌더링 |
| 상태 브로드캐스트 | 30Hz (33ms 간격) | 수신 후 보간 처리 |
| 이동 계산 | 서버에서 최종 위치 결정 | 로컬 예측 + 서버 보정 |
| 스킬 판정 | 서버에서 데미지/효과 계산 | 이펙트 렌더링만 |
| 치트 방지 | 입력 검증 + 레이트 리밋 | - |

**장점**: 치트 방지, 모든 클라이언트의 상태 일관성 보장, 서버 단일 소스 오브 트루스(Single Source of Truth)

### 3.3 적응형 보간 시스템 (Adaptive Interpolation)

네트워크 지연 환경에서도 부드러운 게임플레이를 제공하기 위해 직접 설계한 보간 시스템입니다.

```
서버 업데이트 간격 측정 (EMA: 기존 70% + 새값 30%)
      │
      ▼
┌─ 보간 지속시간 = 서버 업데이트 간격 × 1.15 ─┐
│                                               │
│  이동 중: 로컬 예측 + 점진적 보정             │
│  정지 중: 데드존(5px) 내 고정, 초과 시 40% lerp│
│  위치 차이 > 200px: 즉시 스냅                 │
│  스킬 시전 중: 서버 위치 100%                  │
└───────────────────────────────────────────────┘
```

- **양방향 위치 보정**: 서버→클라이언트 + 클라이언트→서버 동시 보정
- **이징 함수**: easeOutCubic으로 자연스러운 움직임
- **적응형 타이밍**: 실제 네트워크 상태에 따라 보간 파라미터 자동 조정

### 3.4 프로젝트 디렉토리 구조

```
defence_game/
├── src/                          # 클라이언트 (React)
│   ├── admin/                    # 관리자 대시보드 (7개 페이지)
│   ├── components/               # React 컴포넌트 (70+개)
│   │   ├── canvas/               # Canvas 렌더링 (RTS/RPG)
│   │   ├── screens/              # 화면 컴포넌트 (15개)
│   │   ├── ui/                   # UI 오버레이·패널 (35+개)
│   │   ├── touch/                # 모바일 터치 컨트롤
│   │   └── common/               # 공용 컴포넌트
│   ├── constants/                # 게임 설정 (난이도, 스킬, 스탯)
│   ├── effects/                  # 파티클 이펙트 시스템
│   ├── game/                     # 게임 로직
│   │   ├── rpg/                  # RPG 시스템 (10개 모듈)
│   │   ├── units/                # RTS 유닛 시스템
│   │   └── ai/                   # AI 컨트롤러
│   ├── hooks/                    # 커스텀 훅 (14개)
│   ├── renderer/                 # Canvas 2D 렌더러 (11개 모듈)
│   ├── services/                 # API·WebSocket 클라이언트
│   ├── stores/                   # Zustand 상태 관리 (9개 스토어)
│   ├── types/                    # 클라이언트 타입 정의
│   └── utils/                    # 유틸리티 함수
│
├── server/                       # 서버 (Node.js, npm workspace)
│   └── src/
│       ├── api/                  # REST API 라우터
│       │   └── admin/            # 관리자 API (7개 라우터)
│       ├── game/                 # 서버 게임 엔진 (11개 모듈)
│       ├── room/                 # 방·로비 관리
│       ├── websocket/            # WebSocket 서버·메시지 핸들러
│       ├── friend/               # 친구·DM·초대 시스템
│       ├── middleware/           # 인증·검증·레이트 리밋
│       ├── services/             # Supabase 통합
│       ├── state/                # 서버 글로벌 상태
│       └── utils/                # 유틸리티 (비속어 필터 등)
│
└── shared/                       # 서버-클라이언트 공유 타입
    └── types/
        ├── game.ts               # RTS 게임 타입
        ├── network.ts            # 네트워크 메시지 타입
        ├── rpgNetwork.ts         # RPG 협동 모드 타입
        ├── hostBasedNetwork.ts   # 멀티플레이어 직렬화 타입
        └── friendNetwork.ts      # 친구·DM 타입
```

---

## 4. 주요 기능 상세

### 4.1 RPG 모드 (메인 콘텐츠)

#### 클래스 시스템 (4 기본 + 8 전직)

```
Warrior (전사)  ─┬─ Berserker (광전사)  : 공격·공속 특화, 피해흡혈
                 └─ Dark Knight (암흑기사): HP 소모형 고화력, 토글 스킬

Archer (궁수)   ─┬─ Sniper (저격수)     : 단일 타겟 극딜, 50% 치명타
                 └─ Ranger (레인저)      : 다중 타겟, 고속 연사

Knight (기사)   ─┬─ Guardian (가디언)   : 피해 30% 감소, 팀 보호막
                 └─ Paladin (팔라딘)    : 기본공격 시 아군 힐, 부활 지원

Mage (마법사)   ─┬─ Archmage (대마법사) : 보스 데미지 x1.5, 광역 화력
                 └─ Healer (힐러)       : 지속 힐 오라, 전체 회복기
```

- **전직 조건**: 캐릭터 레벨 15 이상 달성
- **2차 강화**: 캐릭터 레벨 40 달성 시 스탯 1.2배 증가
- **패시브 성장**: 레벨 5부터 클래스별 고유 패시브 자동 성장

#### 스킬 시스템

각 클래스는 Q(기본기), W(특수기), E(궁극기) 3개 슬롯을 사용합니다.

| 스킬 | 특징 | 쿨다운 범위 |
|------|------|------------|
| **Q** (기본기) | 기본 공격 대체, 클래스별 고유 효과 | 공격속도 기반 |
| **W** (특수기) | 돌진/범위 공격/회복 등 | 4~8초 |
| **E** (궁극기) | 전투 판도를 바꾸는 강력한 스킬 | 30~60초 |

- 전직 시 W/E 스킬이 상위 스킬로 교체
- 특수 쿨다운 감소: 가디언·팔라딘 기본공격 적중 시 W 쿨다운 1초 감소
- 마법사 계열 패시브: 쿨다운 최대 50% 감소

#### 인게임 업그레이드

| 업그레이드 | 효과 | 골드 비용 |
|------------|------|----------|
| 공격력 | +5/레벨 | 50 × (레벨+1) |
| 이동속도 | +0.08/레벨 | 50 × (레벨+1) |
| 체력 | +50/레벨 | 50 × (레벨+1) |
| 공격속도 | -0.03초/레벨 | 50 × (레벨+1) |
| 골드 수급량 | +1/초/레벨 | 50 × (레벨+1) |
| 사거리 | +8/레벨 (최대 10) | 50 × (레벨+1) |

#### 적 시스템 & 웨이브

- **4종 일반 적**: 근접, 원거리, 기사, 마법사
- **웨이브 진행**: 10웨이브마다 보스 등장, 웨이브별 적 조합 변화
- **보스 2종**: 기본 보스 + 다크 메이지 (지옥·묵시록 난이도 전용)
- **보스 스킬**: 내려찍기, 돌진, 충격파, 암흑 유성, 영혼 흡수, 공허 지대 등

#### 난이도 시스템 (6단계)

| 난이도 | 권장 레벨 | 적 체력 | 적 공격력 | 보상 배율 |
|--------|----------|---------|----------|----------|
| 쉬움 | 1+ | 1.0x | 1.0x | 1.0x |
| 보통 | 15+ | 2.2x | 1.6x | 1.3x |
| 어려움 | 25+ | 2.8x | 2.2x | 2.0x |
| 극한 | 40+ | 4.0x | 3.0x | 2.8x |
| 지옥 | 55+ | 4.5x | 3.3x | 3.2x |
| 묵시록 | 70+ | 6.0x | 4.0x | 4.2x |

#### 넥서스 디펜스 & 승리 조건

- **넥서스**: 맵 중앙, HP 5000, 레이저 자동 방어 (200px 사거리)
- **적 기지 4개**: 상하좌우 배치, 각 HP 3000
- **승리**: 적 기지 4개 모두 파괴 + 보스 처치
- **패배**: 넥서스 HP가 0이 되면 즉시 패배

### 4.2 RTS 모드

- **게임 시간**: 10분 제한
- **자원 5종**: 금, 나무, 돌, 약초, 수정
- **유닛 생산**: 근접, 원거리, 기사, 마법사, 힐러, 자원 채집 유닛
- **기지 업그레이드**: HP 강화, 수입 증가
- **지뢰 설치**: 방어 구조물
- **AI 대전**: 쉬움~극악 4단계 AI
- **1v1 PvP**: 방 코드 기반 매칭

### 4.3 멀티플레이어 시스템

#### 협동 모드 (Co-op)

- **인원**: 1~4인 (인원수별 적 체력 스케일링: 1.0x → 2.5x)
- **방 시스템**: 6자리 코드 기반, 공개/비공개 설정
- **로비 채팅**: 대기 중 실시간 채팅 (최대 50개 메시지 기록)
- **준비 체크**: 전원 준비 완료 시 호스트가 시작
- **호스트 위임**: 호스트 퇴장 시 자동 위임
- **어그로 시스템**: 기사 2.0x, 저체력 1.5x, 현재 타겟 유지 1.2x

#### 버프 공유

| 클래스 | 공유 효과 | 범위 |
|--------|----------|------|
| Berserker | 주변 아군 공격력 +20% | 200px |
| Guardian | 전 아군 피해 50% 감소 (5초) | 전체 |
| Paladin | 기본공격 시 주변 아군 힐 | 150px |
| Healer | 지속 회복 오라 | 150px |

### 4.4 소셜 시스템

- **친구 추가/삭제**: Supabase DB 기반 양방향 친구 관계
- **온라인 상태**: 로그인/로그아웃/게임 참여 실시간 알림
- **1:1 DM (다이렉트 메시지)**: 대화 기록 관리
- **게임 초대**: 친구에게 방 코드와 함께 초대 전송 (5분 TTL)

### 4.5 계정 & 프로필

- **Supabase 인증**: 이메일/비밀번호 회원가입·로그인
- **게스트 모드**: 비로그인 플레이 지원
- **캐릭터 레벨**: 경험치 누적, 클래스 잠금 해제
- **클래스별 레벨**: 독립적 성장, 전직 조건
- **SP 스탯 투자**: 6개 스탯에 포인트 분배
- **랭킹 시스템**: 난이도별·인원별 클리어 타임 랭킹

### 4.6 관리자 대시보드

- **실시간 모니터링**: 접속자 수, 진행 중인 게임, 서버 상태
- **플레이어 관리**: 검색, 상세 조회, 밴(임시/영구)
- **유저 피드백 관리**: 피드백 목록 열람 및 처리
- **점검 모드**: 예약 점검 알림, 서버 점검 on/off
- **WebSocket 실시간**: 관리자 전용 WebSocket 구독

### 4.7 모바일 대응

- **반응형 스케일링**: viewport meta `width=1280`으로 데스크톱 UI 비례 축소
- **가상 조이스틱**: 터치 기반 캐릭터 이동
- **터치 스킬 버튼**: Q/W/E 스킬 터치 조작
- **가로 모드 안내**: 세로 모드 시 가로 전환 안내 표시
- **전체화면 지원**: 전체화면 전환 시 viewport 재적용

---

## 5. 기술적 도전과 해결

### 5.1 실시간 멀티플레이어 동기화

**문제**: 4명의 플레이어가 동시에 조작하는 환경에서 모든 클라이언트의 게임 상태를 일관되게 유지해야 함

**해결**:
- **서버 권위 모델** 채택 — 서버가 유일한 게임 상태 소유자
- 클라이언트는 입력만 전송, 서버가 60fps로 게임 로직 실행
- 33ms(~30Hz) 간격으로 전체 게임 상태 직렬화 브로드캐스트
- 입력 큐 시스템으로 네트워크 지연 시에도 입력 손실 방지

### 5.2 네트워크 지연 환경에서의 부드러운 움직임

**문제**: 30Hz 상태 업데이트로는 캐릭터 움직임이 끊겨 보임

**해결**:
- **적응형 보간 시스템** 직접 설계 — 실제 서버 업데이트 간격을 EMA로 측정
- **클라이언트 로컬 예측**: 이동 방향 기반으로 다음 위치를 로컬에서 예측
- **양방향 위치 보정**: 서버→클라이언트(점진적 lerp) + 클라이언트→서버(30% 보간)
- **상태별 최적 전략**: 이동/정지/스킬시전/스턴 각각 다른 보간 로직 적용
- **easeOutCubic 이징**: 기계적인 선형 보간 대신 자연스러운 감속 곡선

### 5.3 서버 성능 최적화

**문제**: 4인 멀티플레이에서 60fps 틱 레이트 유지 필요

**해결**:
- **스킬 참조 캐싱**: `hero.skills.find()` 반복 호출 제거 → `_skillQ/_skillW/_skillE` 직접 참조
- **타임스탬프 캐싱**: `Date.now()` 틱당 1회만 호출 → `state.currentTickTimestamp`
- **거리 계산 최적화**: `Math.sqrt()` 제거, `distanceSquared()`로 범위 비교
- **브로드캐스트 최적화**: `JSON.stringify()` 1회 → `sendPreStringifiedMessage()`로 N명에게 전송
- **인플레이스 배열 처리**: `.filter()` → 역순 `for` + `splice()`로 GC 부담 감소
- **입력 큐 최적화**: `Array.shift()` O(n) → 인덱스 순회 + `length = 0`

### 5.4 복잡한 게임 시스템 간 동기화

**문제**: 서버와 클라이언트에 동일한 게임 로직이 존재하면서 미세한 차이가 버그를 유발

**해결**:
- **shared/types/** 디렉토리로 서버-클라이언트 간 타입 공유
- **쿨다운 동기화 전략**: 3초 이상 차이날 때만 로컬 값 우선 (방금 스킬 사용 시), 그 외 서버 값 사용
- **실행 순서 보장**: `updateBuffs()` → `updateSkillCooldowns()` 순서 엄격 준수
- **클라이언트 이펙트 분리**: 서버는 판정만, 클라이언트는 시각 효과만 처리

### 5.5 모바일 크로스 플랫폼 대응

**문제**: CSS 반응형으로는 게임 UI를 모바일에 맞추기 어려움, CSS zoom은 Safari에서 깨짐

**해결**:
- **viewport meta 동적 변경**: 모바일 감지 시 `width=1280`으로 설정 → 브라우저가 자동 비례 축소
- 데스크톱 UI를 그대로 유지하면서 모바일에서도 동일한 레이아웃 제공
- `screen.width/height`로 디바이스 감지 (viewport meta 영향 없음)
- 전체화면 전환 시 viewport meta 재적용 (150ms + 500ms 리트라이)

---

## 6. 코드 품질 & 설계 원칙

### 아키텍처 패턴

| 패턴 | 적용 위치 | 설명 |
|------|----------|------|
| **Server Authority** | 게임 엔진 전체 | 서버가 단일 진실 소스 |
| **Singleton** | EffectManager, SoundManager, FriendManager | 전역 인스턴스 관리 |
| **Factory** | unitFactory.ts | 유닛 타입별 생성 로직 분리 |
| **Strategy** | 적 AI, 보스 스킬 | 타입별 다른 행동 패턴 |
| **Observer** | WebSocket 메시지 → Store 업데이트 | 이벤트 기반 상태 갱신 |
| **Module** | 공유 타입(shared/types) | 서버-클라이언트 타입 일관성 |

### 보안

- **입력 검증**: 방향 벡터 정규화, 좌표 범위 검증, 스킬 슬롯 화이트리스트
- **레이트 리밋**: 행동별 최소 간격 (이동 15ms, 스킬 100ms, 업그레이드 200ms, 방 생성 3초)
- **인증**: JWT 토큰 + bcrypt 비밀번호 해싱
- **밴 시스템**: 임시/영구 밴, 자동 만료 처리
- **비속어 필터**: 닉네임·채팅 메시지 필터링
- **HTTP 보안**: Helmet 미들웨어로 보안 헤더 자동 설정
- **중복 로그인 방지**: 동일 계정 동시 접속 차단

---

## 7. 프로젝트 통계 요약

| 지표 | 수치 |
|------|------|
| 개발 기간 | 33일 |
| 총 커밋 | 388회 |
| 일 평균 커밋 | ~12회 |
| TypeScript 파일 수 | 206개 |
| 총 코드 라인 | ~68,800줄 |
| React 컴포넌트 | 70+개 |
| 커스텀 훅 | 14개 |
| Zustand 스토어 | 9개 |
| 서버 게임 시스템 모듈 | 11개 |
| REST API 라우터 | 11개 |
| 공유 타입 모듈 | 5개 |
| 게임 모드 | 2개 (RTS + RPG) |
| 플레이어블 클래스 | 12개 (4 기본 + 8 전직) |
| 난이도 단계 | 6단계 |
| 보스 종류 | 2종 (10+개 보스 스킬) |
| 관리자 페이지 | 7개 |

---

## 8. 핵심 차별점

1. **서버 권위 실시간 멀티플레이어**: 치트 방지와 상태 일관성을 보장하는 정통 게임 서버 아키텍처
2. **적응형 보간 시스템**: 네트워크 상태에 자동 적응하는 커스텀 보간 알고리즘 직접 설계
3. **풀스택 1인 개발**: 프론트엔드·백엔드·게임 엔진·DB·배포까지 전 영역 단독 구현
4. **듀얼 게임 모드**: RTS + RPG 두 장르를 하나의 코드베이스에서 구현
5. **심층적인 게임 시스템**: 12개 클래스, 6단계 난이도, 보스 AI, 패시브 성장 등 상용 게임 수준의 콘텐츠 깊이
6. **모바일 크로스 플랫폼**: viewport meta 기반 반응형으로 추가 CSS 없이 모바일 대응
7. **관리자 도구**: 실시간 모니터링·플레이어 관리·점검 모드 등 운영 도구 포함
8. **소셜 기능**: 친구·DM·게임 초대 등 소셜 시스템 완비

---

*문서 작성일: 2026-02-17*
*프로젝트 버전: v1.24.5*
